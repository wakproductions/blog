---
pageable: true
per_page: 10
---

- if paginate && num_pages > 1
  %p= "Page #{ page_number } of #{ num_pages }"

  - if prev_page
    %p= link_to('Previous page', prev_page)

%h2 Blog Posts
-page_articles.each_with_index do |article, i|
  %h4= "#{article.date.strftime('%m/%d/%Y')} - #{link_to(article.title, article)}"
  -#= "#{article.date.strftime('%m/%d/%Y')} #{article.body}"

- if paginate
  - if next_page
    %p= link_to('Next page', next_page)
